## Why

<!-- Explain the motivation for this change. What problem does this solve? Why now? -->

## Open Questions & Assumptions

<!-- No human is available to answer clarifying questions live. List each question
     you would normally ask one-at-a-time, followed by the assumption you're making
     to proceed. Be explicit — these are the things a reviewer should double-check first. -->
- Q: <clarifying question> → Assumption: <what you assumed and why it's reasonable>

## Alternatives Considered

<!-- List 2-3 approaches to solving this problem, not just the one you picked.
     For each: one line on what it is, one line on the main trade-off.
     Mark your recommended approach and say why in one sentence. -->
1. **<Approach A>** (recommended): <trade-off>
2. **<Approach B>**: <trade-off>
3. **<Approach C>**: <trade-off>

## What Changes

<!-- Describe what will change. Be specific about new capabilities, modifications, or removals. -->

## Capabilities

### New Capabilities
<!-- Capabilities being introduced. Replace <name> with kebab-case identifier (e.g., user-auth, data-export, api-rate-limiting). Each creates specs/<name>/spec.md -->
- `<name>`: <brief description of what this capability covers>

### Modified Capabilities
<!-- Existing capabilities whose REQUIREMENTS are changing (not just implementation).
     Only list here if spec-level behavior changes. Each needs a delta spec file.
     Use existing spec names from openspec/specs/. Leave empty if no requirement changes. -->
- `<existing-name>`: <what requirement is changing>

## Impact

<!-- Affected code, APIs, dependencies, systems -->
